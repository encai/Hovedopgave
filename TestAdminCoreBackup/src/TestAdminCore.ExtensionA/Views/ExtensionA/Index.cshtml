<ul>
    <li><a href="/extensions/userrole/">Start</a></li>
    <li><a href="/extensions/userrole/addrole">tilføj rolle</a></li>
    <li><a href="/extensions/userrole/removerole">Fjern rolle</a></li>
</ul>
<hr />

<h3>Liste over roller</h3>
<div>
    @foreach (var role in Model)
    {
        <p>
            <strong>@role.Name | </strong>
        </p>
    }
</div>
<hr />
<h4>Hent en brugers roller</h4>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <p>
        Username : @Html.TextBox("UserName")
        <input type="submit" />
    </p>
}
@if (ViewBag.RolesForThisUser != null)
{
    <div style="background-color:yellow;">
        <h3>Roles for this user </h3>
        <ol>
            @foreach (string s in ViewBag.RolesForThisUser)
            {
                <li>@s</li>
            }
        </ol>
    </div>
}